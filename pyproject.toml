[project]
name = "golfequipmentwebscraper"
version = "0.1.0"
description = "Tracks golf club prices across retailer sites into a queryable SQLite price history"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "requests>=2.34.2",
]
